# BUYCART backend dependencies
# Version floors (>=) chosen so the project installs cleanly on modern
# Python (3.11–3.13) using prebuilt wheels — no source compilation needed.

fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
python-multipart>=0.0.9
requests>=2.31
aiofiles>=23.2

# 資料庫相關 (Database)
sqlalchemy>=2.0.30
pymysql>=1.1
cryptography>=42.0
